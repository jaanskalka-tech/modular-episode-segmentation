# Data access

Describe here:
- what input data are required,
- which files are included in the repository,
- which files are omitted,
- how restricted data can be requested,
- expected file paths for running the notebook.

## Suggested format

- `data/raw/example_sample.csv` - public sample used for demonstration
- full dataset - available on request / private / external archive
- processed files - generated by notebook and stored in `data/processed/`
